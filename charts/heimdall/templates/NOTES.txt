{{- if eq .Values.operationMode "decision" }}
Heimdall is installed and configured to operate in decision mode, and is available
in the cluster under the

http{{ if .Values.serve.tls }}s{{ end }}://{{ include "heimdall.fullname" . }}.{{ include "heimdall.namespace" . }}.svc.cluster.local:{{ .Values.service.default.mainPort }}

address. To integrate it with your infrastructure components, please follow the existing
guides: https://dadrus.github.io/heimdall/v{{ .Chart.AppVersion }}/guides/, or consult the documentation of
your infrastructure components on how to achieve this.

{{- else }}
Heimdall is installed and configured to operate in proxy mode.

The actual integration depends pretty much on your requirements and setup.
{{- end }}
