crds:
  enabled: true

featureGates: "AdditionalCertificateOutputFormats=true"

webhook:
  extraArgs:
    - --logging-format=json
  featureGates: "AdditionalCertificateOutputFormats=true"

cainjector:
  extraArgs:
    - --logging-format=json

extraArgs:
  - --enable-gateway-api
  - --logging-format=json