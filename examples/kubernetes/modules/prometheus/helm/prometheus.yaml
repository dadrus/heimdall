kube-state-metrics:
  enabled: true
alertmanager:
  enabled: true

prometheus-node-exporter:
  enabled: false
prometheus-pushgateway:
  enabled: false

server:
  service:
    servicePort: 9090
  extraFlags:
    - web.enable-remote-write-receiver