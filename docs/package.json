{
  "name": "heimdall-docs",
  "version": "1.0.0",
  "description": "Heimdall documentation",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "hugo serve -D --logLevel debug --noHTTPCache --disableFastRender -d /tmp/hemdall-docs",
    "build": "hugo --minify -d ./public"
  },
  "author": "",
  "license": "Apache 2.0",
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^10.0.0",
    "bootstrap": "^5.3.0",
    "bootstrap-icons": "^1.8.3",
    "hugo-flexsearch": "1.0.2"
  }
}
