apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "heimdall.fullname" . }}-test-connection"
  {{- if .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
  {{- end }}
  labels:
    {{- include "heimdall.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: proble
      image: busybox
      command: ['wget']
      args: ['http://{{ include "heimdall.fullname" . }}:{{ .Values.service.management.port }}/.well-known/health']
  restartPolicy: Never
