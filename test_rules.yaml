- id: rule:foo
  url: http://foo.bar/<**>
#  methods: # reuses default
#    - GET
#    - POST
  execute:
#    - authenticator: anonymous_authenticator # can be omitted (same as default)
    - hydrator: subscription_hydrator
    - authorizer: allow_all_authorizer
    - mutator: jwt
      config:
        claims: '{"foo": "bar"}'
    - mutator: bla
      config:
        headers:
          foo-bar: bla
    - mutator: blabla
# no on_error (reuses default)