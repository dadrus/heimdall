- id: role:foo
  url: http://foo.bar
  methods:
    - GET
    - POST
  authenticators:
    - id: unauthorized_authenticator
  authorizer:
    id: allow_all_authorizer
  mutators:
    - id: jwt
      config:
        claims: '{"foo": "bar"}'
    - id: bla
      config:
        headers:
          foo-bar: bla
    - id: blabla
  error_handlers:
    - id: authenticate_on_unauthorized_or_forbidden
    - id: default
